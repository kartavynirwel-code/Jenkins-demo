# Server
server.port=8081

# App info (override via env vars: APP_VERSION, APP_ENV)
app.version=1.0.0
app.env=local

# Actuator – expose health & info endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
